[project]
name = "bonitas"
version = "0.1.0"
description = "Vdome API client, token tools, and demo web panel."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "jinja2>=3.1.6",
    "python-dotenv>=1.2.1",
    "python-multipart",
    "requests",
    "uvicorn",
]
